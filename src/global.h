#pragma once

// Global variables (init first in main)

#include <string>

namespace up
{
    // The include directory of this repo
    extern std::string includeDir;

    int initGlobal();
} // namespace up
